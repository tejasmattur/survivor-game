using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossCoin : BaseCoin
{
    // Start is called before the first frame update
    void Start()
    {
        base.Start();
        coinValue = 15;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
