using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HealthCoin : BaseCoin
{
    // Start is called before the first frame update
    void Start()
    {
        base.Start();
        coinValue = 0;
    }

    // // Update is called once per frame
    // void Update()
    // {
        
    // }
}
